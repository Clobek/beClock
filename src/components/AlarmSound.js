import React from 'react';
import ReactDOM from 'react-dom';

export default (props) => {
    return (
        <div className="app__alarm--sound">
        <div className="app__alarm--sound--title">Step 1. Choose an alarm sound</div>
        <div className="app__alarm--sound--body"></div>
        </div>
    );
};