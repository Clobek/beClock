import React from 'react';
import ReactDOM from 'react-dom';

export default (props) => {
    return (
        <div className="app__footer">
            <div className="app__footer-credit">Made by Bryce Belock</div>
        </div>
    );
};